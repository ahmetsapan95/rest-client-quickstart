package org.sqills.rest.client;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeCaseRestClientIT extends CaseRestClientTest {

    // Execute the same tests but in native mode.
}